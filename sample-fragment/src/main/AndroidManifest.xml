<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.speakerboxlite.router.sample.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Router">

        <activity
            android:name="com.speakerboxlite.router.sample.base.StartActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Router"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.speakerboxlite.router.sample.base.HostActivity"
            android:theme="@style/Theme.Router">

        </activity>

        <activity
            android:name="com.speakerboxlite.router.sample.base.HostLandscapeActivity"
            android:theme="@style/Theme.Router"
            android:screenOrientation="landscape">

        </activity>

    </application>

</manifest>